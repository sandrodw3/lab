/**
 * Small helpers to work with filesystem paths.
 *
 * Exposes {@link getBaseName} to extract the last segment of a path,
 * with optional extension removal.
 *
 * @module
 */

export { getBaseName } from './get-base-name.ts'
